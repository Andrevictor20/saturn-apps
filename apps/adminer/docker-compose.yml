name: adminer
services:
  adminer:
    image: adminer:5.4.2
    deploy:
      resources:
        reservations:
          memory: 32M
    restart: unless-stopped
    ports:
    - 8080:8080
    container_name: adminer
