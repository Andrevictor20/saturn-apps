name: linuxserver-nano-wallet
services:
  app:
    image: linuxserver/nano-wallet:1.3.2
    restart: unless-stopped
    environment: {}
    ports:
    - target: 80
      published: 80
      protocol: tcp
