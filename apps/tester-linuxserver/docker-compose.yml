name: linuxserver-tester
services:
  app:
    image: linuxserver/tester:version-66f677b0
    restart: unless-stopped
    environment:
      URL: ''
    ports:
    - target: 3000
      published: 3000
      protocol: tcp
