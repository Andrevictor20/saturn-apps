name: retroarch-web
services:
  app:
    image: inglebard/retroarch-web:latest
    restart: unless-stopped
    environment:
      ROOT_WWW_PATH: /var/www/html
    ports:
    - target: 8080
      published: '80'
      protocol: tcp
